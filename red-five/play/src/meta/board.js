import{globals as a}from"../config/globals.js";export const DEFAULT_INITIALS="YOU",BOARD_ROWS=5;export function cleanInitials(t){return String(t??"").toUpperCase().replace(/[^A-Z]/g,"").slice(0,3)}export function mergeBoard(t,l){const e=a.leaderboard.map(r=>({name:r.name,score:r.score,isPlayer:!1})),n=Math.max(0,Math.floor(Number(t)||0));if(n<=0)return e;const s=cleanInitials(l)||DEFAULT_INITIALS;let o=e.findIndex(r=>n>r.score);for(o<0&&(o=e.length),e.splice(o,0,{name:s,score:n,isPlayer:!0});e.length>BOARD_ROWS;){let r=e.length-1;for(;r>0&&e[r].isPlayer;)r-=1;e.splice(r,1)}return e}
